# Crypto 接口文档

已从旧 `crypto-api-docs` 项目迁移主流交易所 API 文档。当前目录直接按提供方组织，避免额外的 `exchanges/` 中间层：

- `binance/`
- `bybit/`
- `coinbase/`
- `gateio/`
- `hyperliquid/`
- `kraken/`
- `okx/`

后续如果新增链上、数据供应商等非交易所文档，再按需要扩展到 `onchain/`、`data-vendors/` 等目录。
